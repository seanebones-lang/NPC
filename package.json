{
  "name": "cursor-mcp-integration",
  "version": "1.0.0",
  "description": "MCP integration for Cursor with custom agent routing via Vercel app",
  "main": "server/local-mcp-server.js",
  "type": "module",
  "scripts": {
    "dev": "node server/local-mcp-server.js",
    "test": "node --test tests/*.test.js"
  },
  "keywords": ["mcp", "cursor", "ai", "agent"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0"
  }
}
